[tool.black]
line-length = 105
target-version = ["py311"]

[tool.ruff]
line-length = 105

[tool.ruff.lint]
select = ["E", "F", "I", "N"]

[tool.ruff.lint.per-file-ignores]
"logging_utils.py" = ["N802"]  # formatTime must match logging.Formatter API
